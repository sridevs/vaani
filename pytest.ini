[pytest]
testpaths = tests
python_files = test_*.py
markers =
    adapters: tests for the adapters package (external integrations)
    application: tests for the application package (conversation flow)
